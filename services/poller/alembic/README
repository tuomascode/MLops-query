# Alembic
Alembic is a simple database migration software. To create a migration

1) make changes to `src/db/models.py`
2) make sure db is up to date: `uv run alembic upgrade head`
3) run `uv run alembic revision --autogenerate -m "<some_camel_case_message>"`

Then, to apply migration, run 